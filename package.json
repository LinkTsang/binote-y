{
  "name": "binote-y",
  "version": "0.1.0",
  "private": true,
  "description": "y-websocker server for Binote",
  "main": "dist/app.js",
  "scripts": {
    "start": "tsc && node dist/app.js --enable-source-maps",
    "dev": "nodemon --watch src/ --exec \"npm run start\" --verbose -e ts",
    "build": "tsc",
    "watch": "tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Bin Tsang",
  "license": "MIT",
  "devDependencies": {
    "@types/lodash": "^4.14.172",
    "@types/node": "^16.7.13",
    "@types/ws": "^7.4.7",
    "nodemon": "^2.0.12",
    "typescript": "^4.4.2"
  },
  "dependencies": {
    "lodash": "^4.17.21",
    "ws": "^8.2.1",
    "y-leveldb": "^0.1.0",
    "y-protocols": "^1.0.5",
    "yjs": "^13.5.12"
  }
}
